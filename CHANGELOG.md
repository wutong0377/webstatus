# 更新日志

## [v2.5.2] - 2026-08-25

### 布局优化
- **站点详情页**:状态时间线固定高度(300px)滚轮滚动;故障记录固定高度(360px)滚轮滚动,不再撑破排版
- **首页卡片**:站点简介限制最多 2 行(line-clamp),防止长描述把卡片/整行撑高

## [v2.5.1] - 2026-08-25

### 修复
- **主页文字点击出现"输入线"**:全局禁止文本选中(user-select: none),输入框仍可选中;点击不显示多余聚焦框,键盘导航仍显示焦点
- **子域名识别主域到期时间**:二级/三级乃至多级子域自动提取可注册主域(eTLD+1)再查询,如 www.robforum.xyz → 查 robforum.xyz;兼容 com.cn/co.uk 等多段后缀

## [v2.5.0] - 2026-08-25

### 主服务器 IP 保密(重点)
- 公开 API 一律剥离 IP 字段;/site/:id 改用显式字段列表(不 SELECT *),last_ip 绝不返回前台
- 公开错误信息中的 IP 自动打码(maskIp)
- 探测记录站点解析 IP 到 sites.last_ip(迁移 v7),仅后台站点列表/详情可见
- 新增 robots.txt:屏蔽 /admin /install /api 被搜索引擎收录
- HTTPS 部署自动加 HSTS 头

### 站点详情页状态方块图(新增)
- 新增公开接口 /api/public/site/:id/blocks(近 7 天每小时状态聚合)
- 站点详情页新增「状态方块图」区块(7×24 格,悬停显示时间/状态)

### 域名检测(.cn)必须自动获取
- .cn 类域名 WHOIS 优先(whois.cnnic.cn),其他 TLD RDAP 优先,三级兜底
- 增强 WHOIS 解析(兼容 CNNIC Expiration Time 格式)
- **手动到期日期兜底**:站点表单可手动填到期日期(sites.domain_expiry_override,迁移 v7),自动查询失败时用手动值,保证能算剩余天数发提醒;填写信息全部落库保存

### 移除 OCSP 吊销检测
- 删除前台展示行、后台开关、巡检逻辑;DB 列保留兼容

### 站点配置保留 + 表单预填
- 升级不删库 → 站点配置/检测选项全保留
- 表单预填:TCP 端口提示(443/3306)、期望状态码 200、监控模式 HTTP、证书预警 30、域名预警 90、域名到期日期选择器

## [v2.4.0] - 2026-08-25

### 首页与展示修复
- **修复首页不显示站点状态/状态小方格**:`getSitesLatest` 主查询只查基础字段,证书/域名状态改为单独容错查询,表缺失不再拖垮整个公开接口(之前导致接口 500、首页空白)
- **前台故障记录附原因**:站点详情页每条故障展示「成因 / 建议」(错误码解读)
- **站点表单提示 https://**:域名输入框加红字提示,填 HTTPS 才做证书检测
- **证书/域名预警天数加说明**:输入框 placeholder + 灰字解释(剩余 N 天提醒)

### 维护窗口
- **维护窗口到点生效**:巡检读取当前生效的维护窗口,命中站点不再探测、写状态=维修(蓝)、耗时=0ms、不计故障不告警;首页实时显示蓝色
- **维护计划上后台主菜单**:侧边栏新增「维护计划」入口,直达维护窗口管理

### 域名检测(.cn 专项)
- **RDAP 直连 + WHOIS 兜底**:内置常见 TLD(含 .cn/com.cn 等)注册局 RDAP 直连地址,绕开 rdap.org 引导不稳定;仍失败则 WHOIS 文本兜底(whois.cnnic.cn 等)
- **404 不再标红异常**:无法查询时归类为「无法自动获取到期信息」(状态=预警),显示友好提示,不再出现 RDAP_HTTP_404 裸错误码

### 证书检测
- **非 HTTPS 站点跳过证书检测**:不再对 80 端口做 TLS 导致 CERT_TIMEOUT,显示"站点非 HTTPS"
- **检测失败友好展示**:超时等错误显示"证书检测超时"等文案;OCSP 未校验文案明确

### 邮件与告警
- **邮件发送增加日志**:后台服务日志记录每封邮件发送成功/失败,便于排查"邮件不发送"
- (所有故障类型含卡顿:达抖动阈值即发,逻辑已有;若仍不发多为 SMTP 未配置,后台可先发测试邮件验证)

### 新功能:获取最新情况 + 极验
- **前台「获取最新情况」按钮**:SVG 图标,一键触发一轮完整巡检,结果实时刷新
- **极验(GeeTest)人机验证**:后台「告警设置 → 极验人机验证」配置 captcha_id/key(免费计划可用);首次点击免验证,60 秒内再次触发需人机验证;服务端二次校验,IP 限流

### 报表修复
- **修复「h is not function」**:`loadBlocks` 循环变量 `h` 遮蔽 DOM 助手 `h`(选站点后报错),已改名为 `hh`

## [v2.3.0] - 2026-08-25

### 安装向导修复(彻底解决「来源校验失败」)
- **移除安装阶段的 Origin/Host 同源校验**:该校验在 Nginx / 宝塔 / CDN(Cloudflare) 等反向代理环境下,浏览器 Origin 与 Node 侧 Host 头经常不一致(代理改写 Host 为内网地址、或 CDN 转发),连续导致合法安装被误拦截。安装流程是一次性、登录前的流程,且要求管理员提供数据库凭据,跨站 CSRF 诱导完成安装的风险极低 —— 为不影响安装可用性,**不再拦截**。
- 全面检查确认:代码库无其他 Origin/Host 类严格校验;登录/后台接口的 CSRF、限流、权限、2FA 等 403/429 校验均为合法逻辑,不影响安装与正常使用。

## [v2.2.1] - 2026-08-25

### Bug 修复
- **修复安装向导「来源校验失败」**:同源校验在 Nginx/宝塔反向代理下误拦截(Nginx 把 Host 改写为内网地址,与浏览器 Origin 公网地址不一致)。现改为:Origin 与 Host 一致放行、反代场景(Origin 公网 + Host 内网)放行、仅拒绝 Origin 与 Host 均为公网但不一致的真正跨站请求;无 Origin / null / 不可解析一律放行,不再误伤安装流程

## [v2.2.0] - 2026-08-25

### 体验与优化
- **SEO 动态注入**:站点详情页按站点 SEO 字段动态设置 title / description / keywords
- **首页自动刷新修正**:改为递归 setTimeout,刷新间隔配置即时生效
- **首页卡片排版**:按站点数量自适应网格(1 个单列 / ≤3 双列 / 更多三列),卡片等高
- **前台移动端适配**:状态方块格采用响应式网格,不横向溢出
- **最近故障写清原因**:后台仪表盘「最近故障」直接展示错误解读(名称 + 成因)
- **公开 API 缓存头**:`/api/public/*` 增加 Cache-Control,配合 Cloudflare/CDN 边缘缓存提速
- **i18n 补全**:报表中心等页面硬编码中文改为 `t()`,新增 40+ 翻译键(中/英)
- 报表中心 UI 精简重构(移入手动故障入口后)

### 说明
- 部分功能文案仍为中文硬编码(站点编辑增强检测区、OneBot 配置等),后续可继续补全英文
- 动画遵循系统「减弱动态效果」偏好

## [v2.1.0] - 2026-08-25

### 检测能力补全
- **性能细分统计**:DNS / TCP / TLS / TTFB 分段耗时入库(status_history 新增 4 列),站点详情页展示
- **ICMP Ping / TCP 主探测模式**:站点可切换 monitor_mode=http/tcp/icmp,不再只是 HTTP;tcp/icmp 模式同样过 SSRF 校验
- **重定向链校验**:HTTP 探测跟随最多 5 跳重定向(每跳重新 SSRF 校验),检测循环与超限,记录重定向链
- **HTTP 安全头巡检**:HSTS / CSP / X-Frame-Options 等巡检落地,SSRF 安全实现,结果入库 security_checks,缺失自动告警
- **SSL 证书 / OCSP / 域名到期展示**:后台站点列表徽章 + 前台站点详情「安全检测」卡片(签发机构/SAN/有效期/剩余天数/OCSP 吊销状态/域名到期/注册商)
- **站点编辑表单补全「增强检测」区**:DNS/证书/TCP/域名/OCSP/安全头开关 + 监控模式 + 期望状态码 + 预警天数

### 告警体系
- **OneBot QQ 机器人正式通道**:调用 OneBot v11 HTTP 接口发送群消息 / 私聊消息(可选目标),Bearer Token 鉴权,开关可控;后台配置页补充私聊 QQ 字段

### 前台公开页
- **状态方块图**:站点卡片展示近 96 点状态方块格(一个方块一个状态,悬停显示时间/状态)
- **近 7 天可用率**:前台卡片显示站点可用率百分比
- **证书/域名状态角标**:前台卡片显示证书/域名预警与异常
- **维护窗口前台展示**:首页顶部蓝色横幅展示当前生效的维护计划(站点/时段)

### 数据与兼容
- **旧数据库导入兼容**:安装向导模式 B 支持「强制导入」非本系统备份(风险确认后),向下兼容旧库
- **版本管理页增加「导出数据库备份」按钮**:更新前一键备份
- **故障日志状态筛选补全**为 4 项:正常/卡顿/离线/维修

### 数据库迁移
- v4: status_history 增加 dns_ms/tcp_ms/tls_ms/ttfb_ms
- v5: cert_checks 增加 ocsp_status
- v6: sites 增加 check_headers;新建 security_checks 表
- 全部迁移幂等,向下兼容旧库

## [v2.0.0] - 2026-08-25

### 紧急 Bug 修复
- **后台新增「版本更新」页面**:显示当前版本 / Git 提交信息,一键「检查更新」拉取 GitHub 最新版本并跳转下载;加入侧边栏菜单
- **修复手动触发巡检"接口不存在"**:前端误调用 `/run`,后端路由为 `/run-check`,已修正
- **修复站点详情页 延迟/HTTP/最近检测 全空**:`/api/public/site/:id` 未 JOIN 最新探测结果,现已补充 last_* 字段并统一 status
- **修复版权符号乱码**:`©️`(含变体选择符,部分字体显示方框)改为纯 `©`
- **修复延迟曲线颜色不随状态变化**:改用 Chart.js `segment` 逐段按状态着色(正常绿/卡顿橙/离线红/维修蓝),离线与维修延迟按 0ms 展示但分别用红/蓝;同步修正 tooltip
- **手动故障事件入口迁移**:新建/编辑/删除入口移到「故障日志」页,报表中心移除旧入口
- **修复会话管理 IP 不显示**:支持 Nginx 反代后取 X-Forwarded-For 真实 IP,剥离 IPv6-mapped 前缀
- **报表中心精简重构**:移除手动故障区块后页面更简洁,修复选站点后的异常

### 体验
- 全部静态资源版本号统一为 v2.0.0 缓存破坏

## [v1.1.2] - 2026-08-25

### 安装阻断 Bug 修复（v1.1.0 / v1.1.1 均无法安装）
- **修复安装向导白屏崩溃 `TypeError: t is not a function`**：浏览器会缓存旧版 `/assets/js/common.js`（最长达 1 天），旧版未导出 `t`/i18n 函数，而安装页直接解构导致崩溃。现所有页面对 `common.js` / `app.css` 增加 `?v=1.1.2` 版本号缓存破坏；安装页改为防御性解构（`WS.t || (k => k)` 等兜底），即使缓存旧脚本也不再崩溃。
- **修复致命缺失文件 `src/data/errorCodes.js`**：v1.1.1 误删了该文件但代码仍 `require` 它，导致安装完成后重启服务即 `MODULE_NOT_FOUND` 崩溃、后台无法登录。已从 v1.0 恢复并补充 v1.1 新增错误码（证书 / OCSP / 域名 RDAP 等），共 54 条。
- **修复数据库迁移失败**：`migrate.js` v2 中 `ADD COLUMN 2fa_secret / 2fa_enabled` 未加反引号，MySQL 语法错误导致迁移中断、`role`/`2fa_*` 等列缺失、登录接口报"Unknown column"。已改用反引号，并把迁移改为**幂等**（先查 `information_schema` 判断列/表存在性），部分迁移过的旧库重启后能自动补齐修复。
- **修复 `auth.js` / `accounts.js` 中 `2fa_*` 列未加反引号**导致的 SQL 解析错误。

### 生产环境适配
- **移除 `cdn.tailwindcss.com` 生产警告**：引入 Tailwind CLI 生产构建（`npm run build:css`），编译产物 `app.css` 随仓库提交，部署无需构建步骤；所有页面改为引用本地编译样式并删除 CDN 脚本，离线 / 内网部署不再依赖 Tailwind CDN。
- **Node 版本要求修正为 ≥ 18**：更新检测 / RDAP 域名查询 / Webhook 推送依赖内置 `fetch`（Node 18+），`engines` 声明与安装向导自检、README 同步更新为 ≥ 18。

### 安全加固
- **安装向导 CSRF 同源校验**：安装阶段（登录建立会话前）对所有写接口增加 Origin/Host 同源校验，防止跨站脚本诱导完成安装（把数据库指向攻击者服务器）。无 Origin 的自动化部署不受影响。

## [v1.1.1] - 2026-08-24

### Bug 修复
- 修复安装向导「模式 B 上传备份恢复」导入失败：备份数据中含分号的字符串（如错误信息"连接失败；请检查"）会被旧分句逻辑误切分导致 SQL 执行失败。现分句逻辑能正确识别字符串字面量内的分号与 `''` 转义引号。

## [v1.1.0] - 2026-08-24

### 探测能力增强
- HTTP 探测支持 DNS / TCP / TLS / TTFB 分段计时，连接钉死已校验 IP（防 SSRF 重绑定）
- 新增 TCP 端口连通检测（MySQL / Redis / SSH 等服务端口）
- 新增 DNS 记录监控（A / AAAA / CNAME / MX 与规则比对）
- 新增 SSL 证书检测：签发机构、SAN、有效期、过期预警（可自定义提前天数）
- 新增 OCSP 证书吊销检测（后台开关）
- 新增域名注册到期检测（RDAP 协议）与到期预警（后台开关）
- 新增 HTTP 安全头巡检（HSTS / CSP / X-Frame-Options 等）
- 支持自定义期望 HTTP 状态码（不限于 200）

### 告警体系升级
- 统一告警入口：告警日志中心（记录每次告警与成功/失败状态）
- Webhook 通用回调（可对接第三方服务）
- OneBot QQ 机器人预留通道（配置 + 空调用，不引入额外依赖）
- 维护计划窗口：设定时间段自动屏蔽告警
- 单个监控项可独立关闭告警
- 告警分级（警告 / 严重 / 紧急）

### 数据、报表与故障事件
- 7 / 30 / 90 天站点可用率统计
- 状态方块图数据
- 手动录入故障事件（说明、处理进度）
- CSV 导出扩展（延迟 / 故障 / 可用率）
- 可配置历史数据自动清理

### 后台管理
- RBAC 多账号权限（超级管理员 / 普通管理员 / 只读访客）
- 2FA 二次验证（TOTP，RFC 6238）
- 会话管理（查看登录设备、强制下线）
- 版本信息 + GitHub 更新检测（仅提示，不自动更新）
- 前台展示 SSL 证书与域名到期状态

### 前端与体验
- 中英文 i18n 切换（覆盖主要页面）
- skeleton 加载占位、站点卡片等高、动效克制
- 修复手机端卡片渲染中断
- Cloudflare / CDN 性能优化文档与 Nginx 压缩示例

### 安全与版权
- 数据库迁移机制（旧库自动升级，向下兼容）
- 页面版权信息（© 梧桐Wutong · GitHub · QQ）+ 哈希完整性校验，篡改即停止运行

## [v1.0.0] - 2026-08-24

- 多站点 HTTP 监控（最多 10 站），四色状态体系（正常 / 卡顿 / 离线 / 维修）
- Web 可视化安装向导（环境自检 / 全新初始化 / 上传 SQL 备份恢复双模式）
- 数据库备份导出与恢复
- SMTP 邮件告警（抖动抑制、冷却、HTML 模板编辑器、多接收邮箱、发送日志）
- 历史数据降采样曲线、状态时间线、故障记录、CSV 导出
- 完整错误码解读库
- 安全加固：CSRF / SSRF / XSS、双重限流、登录锁定、bcrypt、参数化查询
- Nginx 完整配置 + PM2 进程守护 + 宝塔部署教程
