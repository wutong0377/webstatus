feat(v2.5.0): IP保密 / 站点详情方块图 / .cn域名自动获取+手动兜底 / 移除OCSP

主服务器IP保密:
- 公开API一律剥离ip字段, /site/:id改用显式字段列表(不SELECT *)
- 公开错误信息IP自动打码(maskIp)
- 探测记录IP到sites.last_ip, 仅后台可见
- 新增robots.txt屏蔽/admin /install /api; HTTPS自动加HSTS

站点详情页状态方块图(新增):
- 公开接口 /api/public/site/:id/blocks(近7天每小时)
- 详情页新增7x24状态方块图区块

域名检测(.cn)自动获取:
- .cn类域名WHOIS优先(whois.cnnic.cn), 其他TLD的RDAP优先, 三级兜底
- 增强WHOIS解析兼容CNNIC格式
- 手动到期日期兜底(sites.domain_expiry_override), 自动失败时用手动值, 信息落库保存

移除OCSP吊销检测(删前台/后台/巡检逻辑, DB列保留)

站点配置保留(升级不删库) + 表单预填(TCP端口/期望状态码/到期日期等)

迁移v7: sites加last_ip + domain_expiry_override
